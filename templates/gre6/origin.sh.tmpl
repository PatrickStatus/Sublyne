#!/usr/bin/env bash
set -euo pipefail

ip -6 tunnel add "{{ interface }}" mode ip6gre local "{{ ip_origin }}" remote "{{ ip_dest }}" ttl 255 key "{{ key }}"
ip addr add "{{ origin_private_ip }}" dev "{{ interface }}"
ip link set dev "{{ interface }}" mtu 1452
ip link set dev "{{ interface }}" up
